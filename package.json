{
  "name": "sportsmanship-app",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --production=false && npm run build && cd ../frontend_admin && npm install --production=false && npm run build",
    "start": "echo 'This is handled by Procfile'"
  },
  "dependencies": {},
  "devDependencies": {},
  "cacheDirectories": [
    "frontend/node_modules",
    "frontend_admin/node_modules"
  ]
}
